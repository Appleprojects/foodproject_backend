{
    "devCommand": "uvicorn backend:app --host 0.0.0.0 --port 3000",
    "builds": [
      {
        "src": "backend.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "backend.py"
      }
    ]
  }